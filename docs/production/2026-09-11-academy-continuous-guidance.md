# 角色教学持续指引修复

用户反馈：主角出牌后曾短暂闪英文，随后只剩半透明黑色遮罩，不知道继续操作什么。此次重新打开角色教学验收，不将此前规则测试通过等同于实机通过。

实机定位到的断点是目标完成后的练习状态：旧CueMode5放开操作，却停止推荐卡牌和目标，遮罩仍覆盖所有内容。现改为完成目标后继续推荐全队合法牌、目标或结束回合，直到获胜；练习阶段保留玩家自由选择。

动画或目标控件布局过渡时提供可见的战斗/手牌区域，不再出现没有镂空区域的暗屏。提示移到敌方意图右侧，字号和宽度保持适配；遮罩改挂整个viewport根节点，覆盖两侧留边。取消选牌也有明确短提示。

语言：固定中文实测没有复现自动切英文。此前短暂英文可能由本任务在用户试玩中切语言取证造成，已停止并行切换语言或场景；不能将其表述为已经修复了一个确定的游戏自动切语言缺陷。

验证：

- 冷UBT 98.47秒成功；之后只修正一处缩进，不改变代码语义。
- 21/21引导相关检查通过；29课全程使用实际推荐器完成目标并获胜，去掉测试原先在目标完成后自行接管的隐藏AI流程。
- 实机观察主角资源1/3→防护2/3→四式3/3连续通过，第三节完成法术任务/选牌后正常返回桌面。完成目标后的牌与敌人仍有明确高亮。
- 已保存此次手动进度与首通奖励，退出Dev并恢复挂机。保存时15级、557000金币、8箱，仅作时刻记录，后续不得据此回滚。
- 恢复槽：`GameXXK_UserUI_Keep_20260911_025606`。
- 证据：`Saved/Codex/UIGuidanceLocalization-20260910/academy-continuous-tests.json`、`ui-review/academy-practice-continuous-target.png`、`ui-review/approved-user-progress-result.json`。

其余12课程尚未逐门完整鼠标实拍；已通过同一推荐器的29节行为检查。完整项目总目标仍未完成，本轮不提交或上传。
