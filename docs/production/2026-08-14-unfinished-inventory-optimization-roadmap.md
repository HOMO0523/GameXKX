---
status: record
owner: codex
updated_at: 2026-08-14
source_commit: efab9a5
---
# 未完成项盘点与后续优化路线图（2026-08-14）

> 本文盘点当前所有计划中**未完成**的项目，并给出分层优化方案。
> 真源：`docs/production/optimization-plan.md`、`docs/production/current-goal-acceptance.md`、
> `docs/production/2026-08-12-balance-tuning-ledger.md`、
> `docs/superpowers/plans/2026-08-13-gamexxk-idle-desktop-migration-implementation-index.md`、
> `docs/design/2026-08-13-terrain-benefit-redesign.md`（未入库）。

---

## 1. 未完成项清单

### A. 已裁决设计、未实施（玩法级，优先级最高）

| # | 项目 | 现状 | 阻塞点 |
|---|---|---|---|
| A1 | **地形增益重设计** | 用户已于 2026-08-13 裁决核心规则（每回合全员基础 1 次 + 对应职业再 1 次 + 山河套再 1 次；敌方全体 1 易伤+1 燃烧）。§3 数值表与 §4 职业对应表按提案落地。**代码未动**：`ResolveTerrainBenefit` 仍是旧表；设计文档本身未入库 | ①§5 山河三档细节待最终复核；②`TriggerTerrainBenefit` 卡牌口径待复核 |
| A2 | **数值迭代队列**（平衡台账 §6） | 迭代 1-4 已完成；月白首领 33.3%、周光祖首领 16.7%，仍远低于中期首领 60–80% 目标；弓手 Boss 93% 过强；后期成长档 64% 偏低 | 按台账顺序继续单变量审计 |

### B. 已规划未实施（大功能包）

| # | 项目 | 现状 | 阻塞点 |
|---|---|---|---|
| B1 | **历练桌面迁移 7 包**（历练核心+存档 / 在线宝箱账本 / 2D 历练界面 / 桌面迷你窗 / 自动战斗2× / 任务NPC显式入队 / 默认入口切换） | 全部未开工，仅索引+7 份计划 | **版本边界过期**：索引按 v16/v17/v18 规划，但奖励重构已把 `CurrentSaveVersion` 提到 **17**，需重排为 v17→v18（历练核心）、v18→v19（NPC 语义）、v19→v20（默认入口）后才能执行；且是否仍按此方向推进需用户拍板 |

### C. 项目自身优化计划未完成（`optimization-plan.md`）

| # | 项目 | 现状 |
|---|---|---|
| C1 | 2.2 `WITH_DEV_AUTOMATION_TESTS` 收敛 | 暂缓：503 处 `*ForTest` 遍布 30+ 生产头文件，需单独立项（接口重设计 + Shipping 构建回归） |
| C2 | 3.1 新旧状态合并 | 仅规划：`FGameXXKRuntimeState`/`FGameXXKSaveState` legacy 重复字段与 Deprecated 入口清理，需迁移+全量回归 |
| C3 | 3.2 巨型文件拆分 | 仅规划：`GameXXKCardTypes.h` ~2400 行、`GameXXKBattleBoardWidget.h` ~1200 行 |
| C4 | 3.3 数据 DataAsset 化 | 仅规划（架构决策，需与确定性+单测原则平衡） |
| C5 | 3.4 测试盲区 | 仅规划：整局路线通关率模拟（累计掉血/休息/三章连乘）、Widget 像素级验收 |

### D. 文档/流程/仓库收尾

| # | 项目 | 现状 |
|---|---|---|
| D1 | `current-goal-acceptance.md` 滚动指针过期 | 仍写着"战斗奖励分层（新规格）"为待办（实际已完成并提交 `00002f1`）；应更新为奖励体系重构完成并指向本路线图 |
| D2 | 奖励重构 PIE 真机终验 | 战斗入场/牌库装配/出牌/回合流转已在 PIE 验证；**完整首领战演出（击杀首领→奖励框→点选首领牌入槽入手牌）未在真机走完**，建议真人手动验收 |
| D3 | 仓库卫生 | 202 个未跟踪文件：16+ 探针脚本（`_probe_*`）、临时文件（`test_inv_09b.out`、`tmp_diag.jsx/txt`）、美术/动画过程文件、`ui_psd_pipeline` 脚本、孤儿生成头 `Source/GameXXK/Public/UI/Generated/GameXXKPage07Layout.gen.h`、未入库设计文档 `docs/design/2026-08-13-terrain-benefit-redesign.md` |

---

## 2. 优化路线图

### Phase 0 —— 零风险收尾（0.5 天，建议立即）

1. 更新 `current-goal-acceptance.md`：奖励重构完成、7 包与地形重设计状态、指向本路线图。
2. 地形重设计文档入库（`git add` 该设计文档）。
3. 仓库卫生：`_probe_*` 一次性探针移入 `scripts/_archive/` 或 `Content/Python/_archive/`；删除 `test_inv_09b.out`、`tmp_diag.*`；孤儿 `GameXXKPage07Layout.gen.h` 归档（确认无引用，已 grep 无引用）。
4. 奖励重构 PIE 真人验收：起一次 PIE，真人走完一场首领战并点选首领牌（我陪跑驱动，只差最后一击与奖励框点击）。
5. 修订历练索引的存档版本边界（v17→v18 / v18→v19 / v19→v20），不改功能代码。

验证：`git diff --check`、`harness_state_validator.py` 无 error、探针归档后 `grep` 无引用。

### Phase 1 —— 地形增益重设计（约 1-2 天，用户已裁决）

- 前置：与用户复核 §5 山河三档细节与 `TriggerTerrainBenefit` 口径（一次确认）。
- 范围（设计文档 §7）：`ResolveTerrainBenefit` 新表；回合开始钩子（全员结算 1 次 + 职业对应 +1 + 山河套 +1）；敌方全体 1 易伤+1 燃烧取代旧逐地势负面表；山河三档重写；旧表退役。
- TDD：先 RED（固定种子回合开始增益断言、职业对应、山河 2/4/6 档、敌方负面统一）→ 最小实现 → 冷 UBT → 聚焦 GREEN → 全量（198 卡 × 七地势套件、`GameXXK.RouteBalance` 14/14、2400+2520 矩阵）→ mutation RED → 独立提交。
- 风险：改变全职业战斗循环，平衡矩阵胜率会整体漂移；修复后立即重跑 2400/2520 记录新基线并更新台账 SHA。

### Phase 2 —— 数值迭代（每项独立 TDD 提交，按台账 §6 顺序）

1. 月白首领循环（当前 33.3%）：单变量审计治疗/循环缺口（沿用迭代 1-2 手法，禁止叠改）。
2. 周光祖首领循环（16.7%）：四选三组合已拆，继续"治疗型/毒爆型"分流审计。
3. 弓手上限审计：Boss 93%/精英 100% 的蓄力/重箭多段贡献，单变量压降。
4. 装备成长预算复核（普通 L1 精英/首领略超目标，优先级低，暂不动）。
5. 完整路线通关率建模（与 C5 合并）：新建路线级模拟（累计掉血/休息/三章连乘），禁止回写单场目标。

### Phase 3 —— 历练桌面迁移（7 包，需用户拍板）

- 前置：修订版本边界（Phase 0.5）+ 用户确认仍按"放置化大转向"推进。
- 顺序不变（索引 §6）：历练核心/存档 → 在线宝箱账本 → 2D 界面 → 迷你窗 → 自动战斗 → NPC 显式入队 → 默认入口+E2E。
- 每包独立 RED→GREEN→冷 UBT→聚焦自动化→独立提交；跨包不变量按索引 §5。
- 预估：每包 1-3 天，共 2-4 周。

### Phase 4 —— 架构债（逐项评审后单独立项）

- C1（ForTest 收敛）→ C2（状态合并，需存档迁移评审）→ C3（巨型文件拆分）→ C5（路线模拟，可与 Phase 2.5 合并）→ C4（DataAsset 化，最重、最后）。
- 每项冷 UBT + 全量回归 + 独立提交；C2 涉及存档必须写迁移并跑迁移矩阵。

---

## 3. 建议执行顺序

```text
Phase 0（收尾+指针+卫生+版本边界修订）      ← 立即，零风险
  → Phase 1（地形增益重设计，已裁决）       ← 玩法核心，先复核 2 个口径
  → Phase 2（数值迭代 1-5）               ← 台账已排好队
  → Phase 3（历练 7 包）                  ← 用户拍板是否推进
  → Phase 4（架构债，逐项立项）
```
