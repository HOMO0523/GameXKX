# 挂机摘要栏刷新与宝箱布局

当前状态：布局已经冷编译和实拍；最后一次正常挂机复核仍抓到文本自身缓存滞后，已补充逐文本重绘及显式布局失效，最新几行增量尚待冷编译和复测。另一新开的交互编辑器占用 Live Coding 锁且未连上 MCP，正在等待用户暂停操作以安全保存重启。下文通过记录不能替代这次待完成验证。

用户在本任务明确批准继续 `codex/ui-visual-optimization`。保留本轮开始前的未提交修改；未提交 Git。

## 修改

- 记录纸条从 420 缩为 412 逻辑像素，移除纸条外层裁剪和额外容器内边距；长文本在文字自身范围内省略，完整保留纸框右边缘。
- 展开宝箱整体下移 38：普通箱 Y=46，高级箱 Y=122，仍为 72×72；高级箱底部距离挂机场景底边 8 像素。同步拖动区域中的按钮排除范围。
- 折叠宝箱采用现有普通/高级箱图标、88×24 纸框和纯数字。两条之间留 8 像素；沿用左键单开、右键全开。折叠时的 Win32 可见及输入区域扩展至 1217，涵盖两条宝箱。
- 波次小栏及数字文本持续重绘，填充和数字变化显式使布局失效，展开及折叠箱数也持续重绘。图标顺序改为读取实际遭遇定义并从右向左排列（当前普通×4、精英×2、首领）。

## 证据

所有界面验证均在 `/Game/GameXXK/Maps/L_DesktopTrainingHUD`。

- 修改前：`Saved/Codex/IdleSummaryFix-20260908/before-refresh.json` 记录内部已到 5/7、显示仍为 4/7，折叠重建后更新。
- 布局版本冷 UBT Editor 构建通过；最新逐文本缓存增量的 `build-final.log` 暂被另一个交互编辑器的 Live Coding 锁阻止。没有使用 Live Coding 或 Hot Reload。
- 10/10 定向 Automation，0 错误、0 警告：`Saved/Automation/IdleSummaryFix20260908Final/index.json`，包括实际 Slate 文本更新、记录状态机、数字刷新、固定停靠与原生区域。
- 展开 12、折叠 10、背包打开且折叠 5，共 27 个稳定样本，屏幕文本均与真实遭遇序号相同；覆盖 7/7→1/7。`open-live.json`、`folded-live.json`、`backpack-folded-live.json`。
- 通过 Slate 实际点击折叠普通宝箱，数量 7→6，出现开箱记录：`chest-click.json` 与 `chest-click-slate.txt`。
- 后续自然挂机：展开 100% 的 8 个样本通过；折叠 100% 第 3 个样本出现内部 4/7、渲染 3/7，见 `final-folded-100.json`。据此增加逐文本缓存处理，不能将此前 27 个样本视为刷新问题最终验收。
- 最后两项内边距相关 Automation 2/2 通过：`Saved/Automation/IdleSummaryPadding20260908/index.json`。50/75/100% 折叠外观已实拍。

以上路径中未写完整前缀的文件均位于 `Saved/Codex/IdleSummaryFix-20260908/`。本轮开始时的三个 C++ 文件备份及本次增量 diff 位于同目录，不将其他任务的修改计入本次工作。

存档保护：测试加载带来的槽 1 文件变化已按本轮开始前备份逐字节恢复（恢复前核对文件未被随后用户操作改写）；Autosave 哈希始终未变。用户随后在新开的编辑器把 HUD 改为 75%，保留这个新选择，不用本轮最初的 100% 覆盖它。见 `save-restored.json`。

## 验证边界

一次性推进 600 个逻辑步的压力探针曾在 Slate 渲染器触发访问异常（`Saved/Crashes/UECC-Windows-DA4BEC844CEA2D15B609B5B5764D2C20_0000`）。该轮还切换过旧战斗存档及 Dev 快照，尚未确定单一根因，不据此宣称引擎压力路径已修复。清洁桌面会话的逐次推进、切换与点击复核未再复现；探针已限制每次最多 25 步。
