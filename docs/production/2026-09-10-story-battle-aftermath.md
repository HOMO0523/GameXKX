# 任务战斗编组与战后收尾

用户批准继续处理六章通跑发现的问题。本轮沿用已经通过的61张插图、角色身份、剧情结果和每章一次独立任务行程；普通关卡不使用任务专属怪物编组。

## 实施

| 节点 | 任务敌人 | 局外战前对白 | 胜利后收尾 |
|---|---|---:|---:|
| S00-04 初次战斗 | 公鸡、黄鼠狼 | 8句 | 6句 |
| S01-07 解救猎人 | 黄鼠狼、公鸡、狸猫 | 8句 | 6句 |
| S03-10 密林遇劫 | 灰狼 | 8句 | 5句 |
| S05-06 英雄救美 | 灰狼、豪猪 | 5句 | 5句 |

敌方单位与开场招式均按这份任务编组建立，保留关卡战斗等级与既有怪物数值规则。救援动作、答谢与后续线索移至真实胜利之后；金贵收好烟杆保护图袋，剧情文字与已批准画面一致。

胜利后任务仍为进行中，读完收尾才完成并开放奖励及依赖它的后续节点。利用现有任务计数保存 `MainStory.BattleWon` 与 `MainStory.PostBattleLine`，不增加存档版本；中途暂停、离开任务图、磁盘保存和读档后，重新点击任务应从未读句继续，不重新进入地图或战斗。旧存档已经完成或领奖的节点继续保持完成，不补设强制对白。

回看对白按“战前→战后”串联，通过原对话框与既有头像显示。启程提示使用“启程”标题，不再让主角读出关卡操作说明。

## 验证状态

- 美术确定性检查通过：61张图的已批准文件哈希、art/result合同均未改变。证据：`Saved/StorySystem/BattleAftermath/art-audit.json`。
- 初轮联合冷编译在两个怪物目录调用的接口名处失败；修正为项目现有 `Find` 接口后，冷增量6动作28.18秒Succeeded。日志：`Saved/Codex/UIGuidanceLocalization-20260910/english-semantics-fixed-build.log`。随后共用本地化初始化顺序修复也完成冷增量4动作65.43秒Succeeded。
- 新增战后暂停/退出/磁盘恢复/旧完成状态回归，扩充编组、回看与全部说话角色头像覆盖；61节点实战通跑增加四次战后中途读档，实际共69次读档。
- 原通跑证据保留于 `Saved/StorySystem/BattleAftermath/before-report.json`；不能用旧的65次读档结果替代本轮验收。

设置占用背包中央主区域由《优化界面引导并完成本地化》实施并共用冷构建。本记录不把100% HUD在有限逻辑高度上的整体裁切问题写成已解决。

## 本轮自动检查结果

Saved/Automation/MainStoryBattleAftermath20260910/index.json：30/30通过、0失败；两条系统联网探测超时警告不影响断言。覆盖所有MainStory、两项真实存储、普通关卡编组目录及三项桌面宿主回归。

Saved/StorySystem/BattleAftermath/report.json：独立50级流程夹具完整走完61节点、6次行程、4场真实出牌战斗，共21个战斗推进动作；61次逐节点读档、4次战斗内读档、4次战后中途读档，共69次。全部节点Rewarded；6100000金币、60普通箱、60高级箱，无重复领取。四场实际敌人与上表完全一致。该夹具跳过挂机升级，不代表低等级战斗平衡验收。

## 实机复核

第一次临时会话的额外推进/两次领奖已由用户确认是其手动操作，不据此判定程序误推进。用户暂停操作后重新验证：从专属问号进入公鸡/黄鼠狼战斗；使用实际自动出牌获得胜利，任务仍Active并处于战后line0；读一句、暂停并退出任务图，再点S00-04，line1保持且没有新地图/新战斗。已实拍行脚山客的战后对白，余下对白完成后奖励领取一次。证据：`Saved/StorySystem/aftermath-resumed-verified.png`、`BattleAftermath/ui-pause-exit.json`、`ui-resume.json`、`ui-finish.json`。

75% HUD原生鼠标左边缘/中央/右边缘/回中检查通过：左端StripX=0，BodyOffsetX=90；回中BodyOffsetX=0，缓存任务面板RenderTranslation逐项匹配，未残留90偏移。三张边界实拍位于`Saved/StorySystem/BattleAftermath/drag-left.png`、`drag-middle.png`、`drag-right.png`；完整断言位于`drag-results.json`。50/100%本轮未重复该实拍。

第二次临时会话已完整恢复原RuntimeState，Dev=false、恢复时434780金币；现行英文及75%缩放保留。18765编辑器已交还界面任务。用户追加敌方意图卡小白字描边问题，定位到78宽的DownOnly数值格与1px描边，该问题正在同一BattleBoard显示段落随英文修正合并处理，结果另行补记。
