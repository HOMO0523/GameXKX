# 演武教学与局外商店实施记录

用户已批准实施。分支沿用本轮 UI 工作分支，根目录开发，不使用 worktree。

## 验收目标

- 基础、六职业、六 NPC 共 13 项独立教学，实际机制目标与战斗胜利共同决定完成。
- 每项首次通关 100000 局外金币；重复进入、重放、读档不重复发放；教学不污染真实阵容与道具。
- 文案使用简明书面表达，清楚说明操作与完成条件。
- 邮件左侧购物车，背包区域九种商品：六套装包各200000、宝石包100000、高级宝箱100000、普通宝箱25000。
- 用户确认：套装包随机获得对应套装的一件装备；宝石包一颗，沿用宝箱规则。
- 追加任务入口：两个旧圆钮改用已有无字底图和运行时江湖体文字；剧情任务在左侧仓库竖向区域打开，与仓库互斥，保留各自状态。
- 任务抽屉遵循 `docs/superpowers/specs/2026-08-29-desktop-2d-narrative-task-drawer-design.md` 的可进行/待领取、列表与固定详情区设计。任务按钮不再直接进入 3D 马车预览；进城按钮保留独立功能。

## 实施顺序

1. 教学目录数据、进度与首通奖励纯规则及测试。
2. 独立教学会话、真实机制观察、退出恢复与持久化。
3. 教学目录/提示/结算 UI，13 项内容与回归。
4. 局外商店目录、交易与购物车/嵌入布局。
5. 冷编译、自动化、真实 PIE 教学和购买验证。

当前：基础代码实现中；尚未发放奖励或完成新功能验收。

## 后续进度（以本节为准）

- 商店六种套装包最新售价均为100000；宝石包/高级宝箱100000，普通宝箱25000。金额改用 `FText::AsNumber`，不再硬编码“万”。装备与商店宝箱等级读取购买时工具等级，未解锁为1级。
- 1～10件批量购买、原子扣款/发放、保存失败回滚、实际物品展示及装备悬停已接入。界面仍持续根据用户截图校准。
- 13项教程（含法师四分支）可构建并经规则模拟完成目标与胜利。逐步指令、遮罩高亮、选牌/目标/结束回合限制的新代码已冷编译通过，真实 PIE 步骤与异常路径尚待验证。
- `Saved/Codex/progressive-guide-build.log`：Succeeded。`Saved/Automation/ProgressiveGuideRules_20260908`：12 passed，0 failed。工作台先前整体回归仍有旧 `InnerGeometry` 失败；相关工具栏三项已修正并通过。
- 十档品质目前只做色板，尚未接入UE装备材质。最后两档为铂金底色＋更明显的局部镭射反光；第十档为小等边三角切面。至宝起逐级加入装备后柔光，卡底按档分色。预览提供100%/50%、动态及柔光开关，已实看50%。
- 仍需完成：装备tooltip词缀语义逐条核对、宝石图标/空孔结构化展示、背包统一江湖体、品质色板确认后的共用材质接入，以及分步教程真实操作验收。不能把色板或规则测试当作这些项目已完成。

## 任务抽屉增量（未编译验收）

用户追加裁决：两个圆钮分别为“任务”和“教程”。任务保留未来任务系统入口；教程承接全部教程任务与教学关卡。取消旧进城圆钮和马车预览。两种面板与仓库共用左侧区域。

马车退役已执行：通过 UE MCP 删除场景中的马车 Rig 和专用 Aftermath 控制器，保存地图并返回默认 2D 地图；删除 17 个专用 C++/测试文件，清除 PlayerController/Workbench/LevelFlow 的旧跳转与输入锁。通过 UE 资源引用检查后删除四张马车图集和专用停车对白。旧存档清理迁移保留。尚未冷编译；不能将静态清理称为运行验收。

- 已找到并查看现有无字素材 `T_MasterV2_NavDiscTask`，两个圆钮改为该纸底加实时文本；保留独立进城入口。
- Workbench 增加任务/仓库互斥、会话字段、侧边面板命中与主体回收范围；教学目录列表和固定详情区已编写。
- 当前旧 StoryCatalog 为空。新列表使用 Academy 目录，启动依赖尚在实现中的 AcademySubsystem；不能视为可玩的成品。
- 后续必须完成教学会话、实际目标观察、奖励持久化，以及任务列表滚动位置保存、50% 可读性、Esc/关闭/仓库往返测试。
- 当前教学奖励为首通结算自动发放，待领取页明确说明；后续若接入剧情手动领奖，必须接权威完成状态与幂等领取事务，不得显示虚构可领取项。

## 装备品质与 tooltip 最新进度（覆盖以上旧状态）

用户已确认十档品质预览。共用装备品质材质、名称/边框流光、纸底及至宝起的柔光已接入 UE，实际背包图标已放大居中。结构化 tooltip、江湖体和宝石图标/空孔已接入；名称标题不缩小，支持文字字号缩小并按屏幕工作区约束高度。

`EquipmentQualityFinal_20260908` 24项自动化通过；最新图标、tooltip稳定性修改的冷编译通过，`EquipmentStableTooltip_20260908` 13项通过。实际2D PIE已核对装备图标和长篇套装 tooltip 的完整排版，截图见装备设计记录。分步教程的真实操作与异常路径验收、所有词缀的战斗执行路径核验仍未完成，不能用本轮UI测试替代。
