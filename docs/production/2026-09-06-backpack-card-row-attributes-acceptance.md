# 背包横排选人、卡组与属性验收

基于已推送的 `f3fe371`，继续用户批准的 `codex/ui-visual-optimization` 根目录分支。本轮改动保留在工作区，未再次提交或推送。

## 完成内容

- 伙伴与NPC均为一排六张竖卡，采用接近战斗卡的比例，在固定分类按钮上方展开；背包保持可见作为上下文，展开期间以局部输入遮罩阻止误点底层。名称在上、等级在左、立绘在右下。
- 主角/伙伴/NPC按钮打开、关闭时位置不变。返回按钮为红色，固定右下。查看和取消均不改变出战编队。
- 属性/装备/卡组三个顶部标签与底部三项逐列同宽、居中对齐。由实际挂载UMG槽位及嵌入缩放计算，最大中心差为0.0000181逻辑单位；已结合三档截图复核。Python直接读取部分原生控件CachedGeometry会得到零值，因此没有把零几何作为对齐或拖动证据。
- 卡组改为206×285的双列卡，恢复190×228角色卡面，放大卡名和费用。卡池数量和已选数量放在应用按钮旁；未使用的卡槽连同尺寸容器一起收起，滚动范围按角色实际卡池计算。属性/卡组页不显示无关的排序按钮。
- 属性页以中文名称显示主角、六种伙伴职业及六名NPC，分成身份等级、气血/内力、攻防速度、经验四组。数值仍读实际装备快照和各自成长记录，不将内部ID当标题。新增13名角色的名称和实际攻防值回归通过。
- 仓库与天赋横纵方向使用固定长度墨迹滑块。天赋滑块固定在可见节点区边缘，避免随15000宽的内部画布移出屏幕；保留淡墨节点区和右侧说明区的功能区分。
- 遗物溢出列表和对话历史补充墨迹滚动样式；已有的背包/卡组、名册、路线和奖励替换列表墨迹表现沿用。背包原有自定义拖动按钮继续工作。

## 验证

- 最终冷UBT成功：`Saved/Codex/BackpackCardRow-20260906/release-build-verified.log`。没有使用Live Coding或Hot Reload。
- 95项回归中91项通过，剩余 `InnerGeometry`、`TravelCombatPresentation`、`TravelPartyAtlasAsyncFallback`、`TravelPartyAtlasFallbackInventory` 的错误消息与之前基线完全一致，没有新增失败。报告：`Saved/Automation/BackpackCardRow_Release_20260906/index.json`。
- 修复了扩展验证中两处跨界面打开后持有失效名册引用的测试夹具，并将旧滚动条路径和旧数量文字边距断言对齐到既有素材/调优布局；没有为测试修改用户已调好的装备槽或数量位置。
- 默认2D桌面实际窗口截图覆盖50/75/100%，并检查主角、守卫、土司首领和宋金宝属性、NPC卡组及两类六卡选择。
- 真实鼠标选择守卫、宋金宝和点击红色返回通过；出战始终保持原刀客。主角卡组取消一张变为7/8，查看守卫后返回仍为7/8，正式牌组保持8张。
- 真实拖动的容器偏移：仓库0→98.77；天赋纵向7275→11405.55；天赋横向7156→10784.20；卡组0→2774.17。四条路径均用截图和实际容器偏移交叉验证。

原始截图、布局状态与拖动记录位于 `Saved/Codex/BackpackCardRow-20260906/`，概要为 `live-verification-summary.json`。代表图片：`release-partners-100.png`、`release-npcs-100.png`、`release-attributes-npc-100.png`、`release-deck-100.png`、`release-talents-100.png`。

原存档、HUD与GameUserSettings恢复至本次开始前哈希，默认地图包未改，未新增活动存档槽。证据 `cleanup-and-integrity.json`。用户已有地图、素材删除和其他工作区修改保持原样。

## 追加：单层半透明选人背景

按用户对双层实拍的反馈，删除六卡下方独立的小纸底。选择伙伴或NPC时，整块原背包使用深棕色调和0.72透明度；透明输入拦截层不绘制纸张，六张卡、三个分类按钮和红色返回按钮保持不透明，全部位于背包范围内。选择或返回后，背包恢复原色和1.0透明度。

仅将土司首领、周光祖在选择卡上的立绘缩到原布局的88%，给等级和“查看/出战”文字留出距离；用户随后要求下移这两张卡图，底部留量从18恢复为10，即保持缩小尺寸并下移8个逻辑单位，与其他卡图底边对齐。原立绘素材和其他角色尺寸不变。

- 冷UBT成功，6项选人/界面关闭回归全部通过：`Saved/Automation/PickerTransparency_20260906/index.json`。
- 默认2D桌面的伙伴/NPC两类界面均重新拍摄50/75/100%截图，实际挂载控件检查确认没有额外纸底，10个局部交互控件均在完整背景内。
- 真实鼠标选择土司首领、周光祖及点击红色返回通过；关闭后透明度和原色恢复，查看操作没有改变出战队伍。
- 本次开始前的存档、HUD和GameUserSettings已恢复，地图哈希保持不变。本次未提交或推送。

本次独立证据：`Saved/Codex/PickerTransparency-20260906/`，概要为 `live-verification-summary.json` 与 `cleanup-and-integrity.json`。最终效果使用 `final-partners-100.png`、`final-npcs-100.png`；`preview-100.png` 为已废弃的双层原型。

两张NPC卡图下移后的追加验证位于 `Saved/Codex/NpcCardLower-20260906/`：冷UBT通过，100%与75%重新实拍；挂载槽位对比确认仅土司首领和周光祖Y坐标增加8，六张卡图的尺寸及其他NPC位置均保持不变。本次存档和设置已恢复。
