# 首战第二回合教学同场续接修正

状态：已实现并验证，main 工作区，无提交、无打包。

## 问题与行为

用户反馈普通怪第二回合未出现治疗/护甲，到了 Boss 才出现。原来小提示的“关闭”直接清掉本场教学开关和配套发牌，因此只会在下一场重新尝试；此外第二回合还可能被缺失的历史“结束回合”标记挡住。

现在第一场普通战斗按两回合衔接：

1. 首回合攻击、查看气力，再由玩家结束回合让敌人行动。
2. 敌人行动后，下一手保证治疗和护甲；优先治疗，再护甲、查看实际护甲图标、介绍自动。
3. 收起操作提示只影响当前小段，原地不重弹，正常结果/目标步骤/回合交接后在同一场续接；不取消配套发牌。
4. 说明提示看完关闭即完成该段阅读；悬停类仍需实际查看后才认可。没有完成说明时收起，也不关闭整场教学。
5. 第二回合依据实际回合推进，不因为旧标记漏记再次先提示结束回合。效果认可、永久卡组不变、快速胜利正常结算的规则保持。

## 验证

- RED：2项新增回归都按预期失败。关闭小提示导致第二回合 `None`；缺少旧标记时第二回合错误返回 `EndTurn`。`Saved/FirstBattleFollowup-20260916/red/index.json`。
- 冷 UBT：`green-build.log` 通过，未使用 Live Coding/Hot Reload。
- 自动化：31/31通过，覆盖10项首战教学、教学箱和挑战流程。新增关闭续接测试走真实 Board 结束回合与敌人演出交接；完整序列测试也覆盖读完后关闭说明。22条无窗口 BattleVisual 提示，0错误。
- 实机：独立 PIE 使用 `L_DesktopTrainingHUD`。用户参与实际操作；第二回合快照显示 `Heal`，手中同时有归元术与横剑守势。随后同一战斗记录到真实回血16、产甲7、ArmorView、Auto、Complete。
- 同场证据：普通1-1，选中开局可达节点3（编制索引2，不等于已打过2场）；进入挑战及教学完成时已访问节点均为 `[0]`，当前节点类型仍为普通 `Battle`。完成时仍在该场第三回合，没有进入Boss。见 `acceptance.json`、`live-observe-Heal-r2.json`、`live-observe-None-r3.json`。
- 原玩家53份文件与上一份试玩8份存档 SHA-256 均保持；旧试玩在重启前已保存。本轮窗口使用新的 `Saved/FirstBattleFollowup-20260916/ReviewUser`，用户仍在操作，保持打开。

修正仅涉及首战引导的关闭与续接，不代表其余局外引导补课链全部完成。旧版已经写入的“本场明确停用”状态不会擅自改写；下一场正常判断未完成目标。
