# 任务战返回、图标奖励与领取动效

状态：已完成本批实现与检查；用户已明确“动效我验收通过了”。main工作区，未提交、未打包。

## 最终行为

- S00-04操作描述改为“进入战斗，击败兽群”，三条提示也移除过时问号节点流程；运行时生成数据和中英本地化同步，插图和美术说明保持。
- 专用任务战返回桌面时，若当前任务仍有战后对白或未领取结果，重新展开对应主线页面；正常接完战后对白后显示结果。已领取、已主动暂停的任务不强行重开。
- 任务详情和结果用既有金币、普通箱、高级箱贴图展示精确数量。结果图标72、数量32号字；详情图标64。不再把不足一万的报酬写成“0万金币”。
- 成功领取后，金币从奖励图案飞向现有金币图标；余额栏不可见时寻找背包Tab。各类箱子寻找对应当前可见入口，包含折叠入口。表现层挂在桌面外层，重建任务内容不会删掉正在飞行的图案。
- 战斗结算按实际普通/高级/悬赏箱分别显示，避免将混合奖励合并到一个箱子图标。确认时表现已发放奖励，不二次发奖。
- 奖励事务仍由原业务保存后提交。失败和重复领取无新动效；动画只负责表现，使用独立表现事件，不消费掉落随机数。长时间隐藏后不积压重播。

## 验证与覆盖

- RED：原任务返回、过时描述及缺失图标共6项断言失败，见 `Saved/StoryRewardFlow-20260916/red/index.json`。
- 冷UBT通过：`final-build.log`。新增测试源码通过`-gather`重新收集编译；没有以“Target is up to date”冒充新测试已编译。
- 首轮62/62相关检查通过，覆盖主线实际战斗/存档全流程、任务返回、奖励事务/表现、挑战、首战和教学箱。
- 最终字体与表现事件调整后33/33受影响检查通过，见`final/index.json`；此前未变更部分沿用62项结果。7条无窗口BattleVisual提示，0错误。
- 奖励测试覆盖精确小额金币、混合箱子图标、纯动效不发币、相同表现事件不重复、面板重建保留动效层、领取保存失败和重复领取。
- 实机任务奖励验证：+100000金币、+10普通箱、+10高级箱与真实库存变化一致，见`claim-mixed.json`和`acceptance.json`。实际窗口确认大图标、来源爆出及飞行表现；用户已验收。截图：`story-rewards-before.png`、`story-reward-burst.png`、`story-reward-flight.png`。
- 任务返回由完整业务流程及Workbench重开测试覆盖；本轮实机主要检查任务奖励表现。额外手造任务战回放快照因前置故事状态不一致被校验拒绝，没有作为成功证据。

## 验证会话恢复

验证结束已退出临时会话并恢复进入会话前快照。恢复瞬间的金币、背包、装备、天赋、任务与箱子令牌逐项一致，见`acceptance.json`。主项目53份原始存档SHA-256保持不变。之后用户继续在当前试玩窗口操作，未再次重置。

独立试玩档的临时会话期间仍出现手动保存写入：现有`SaveCurrentGame`没有检查开发写保护，而部分界面引导调用该路径。已备份这些临时文件到`session-save-backup`，恢复原快照并保存，读回主槽、检查点及回滚副本，确认S00-04仍为原有已领取、S00-07仍为原有待领取，测试收益未保留。见`review-saves-write-audit.json`、`restored-save-audit.json`。此开发工具隔离问题未混入本批运行时改动，后续验证应优先使用完全独立的UserDir。
