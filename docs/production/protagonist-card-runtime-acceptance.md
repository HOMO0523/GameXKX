# 主角卡牌运行时最终验收

日期：2026-08-11

分支：`main`

验收范围：主角 36 张卡、共享局内机制、等级解锁与存档迁移、自动结算队列、确定性平衡观测、真实 PIE 主链和既有战斗 HUD。

UI 边界：没有修改已确认的页面布局、slot、anchor、size 或既有页签；本轮只补规则、测试、诊断与探针。

## 1. 结论

本轮主角卡功能与运行时完整性通过验收：主角 36 张目录、`8 + 5 + 3` 队伍牌源、灵动、反击/格挡、自动重放、冲锋/收招、护甲转伤、药效/毒爆、重箭、八牌法术任务、六地势收益、同死玩家胜利、暂停选择后存读档续算均有精确测试覆盖。最终冷 UBT 成功；独立命令行最终回归为主角卡 112/112、整合场景 10/10、存档 11/11，均为 0 failed、0 not-run、0 error。

确定性与可玩性探针也通过：三次各 2,400 场输出字节一致，未出现目标合法但卡牌卡手，自动队列与手牌没有越界；真实 PIE 从主菜单走到青山城镇、接任务、永久商店真实购买、手动保存、路线图和局内战斗，Board 血条/内力/状态区结构可见。

这里的“通过”只代表本轮实现和模板规则正确，不代表全项目数值已经最终平衡。当前观测仍暴露成型档 100% 胜率、裸装首领档 0% 胜率等后续调参问题，不能据此宣称所有职业/套装强度已经完成最终收敛。

## 2. 冷编译与最终回归

### 2.1 编辑器关闭门禁

- 通过 UE MCP 只读探针确认：`dirty_content_packages=[]`、`dirty_map_packages=[]`。
- 正常停止 PIE，调用 `save_dirty_packages` 后仍无脏包。
- 通过 `QUIT_EDITOR` 优雅退出；未强杀编辑器。

### 2.2 冷 UBT

命令：

```powershell
& 'D:\UE_5.8\Engine\Build\BatchFiles\Build.bat' GameXXKEditor Win64 Development 'D:\UE5 demo\GameXXK\GameXXK.uproject' -WaitMutex -NoHotReload -NoHotReloadFromIDE
```

结果：`Result: Succeeded`，输出目标为最新；未使用 Live Coding 或 Hot Reload。

### 2.3 最终独立 Automation

| 报告 | 发现/执行 | 结果 | 说明 |
|---|---:|---:|---|
| `Saved/Automation/Task12_FinalAudit_HeroIntegration_20260811/index.json` | 10 | 10 成功，0 失败 | 7 clean + 3 with warnings，0 error |
| `Saved/Automation/Task12_FinalAudit_HeroCards_20260811/index.json` | 112 | 112 成功，0 失败 | 96 clean + 16 with warnings，0 error |
| `Saved/Automation/Task12_FinalAudit_SaveGame_20260811/index.json` | 11 | 11 成功，0 失败 | 11 clean，0 error |

带 warning 的用例来自既有敌方攻击/战斗诊断日志；Automation 状态均为 Success，断言错误数为 0。验证脚本还执行：

```powershell
python -m unittest scripts.test_card_balance_observation scripts.test_gamexxk_real_play_flow_mcp
```

结果：67/67 通过。

## 3. 十个跨机制整合场景

`GameXXK.Data.HeroCards.Integration` 精确覆盖：

1. 刀客自动重放弓手牌时，蓄力只由真实重箭动作锁定并消耗一次。
2. 法师回放阵师牌不误触主动换场监听，也不增加主动出牌计数。
3. 法师火任务队列中的药师毒爆可终结战斗，且不会留下半提交队列。
4. 敌方多段攻击全部被灵动闪避后，每个独立反击/格挡来源仍只在整张敌牌边界触发一次。
5. 战斗牌源严格保持主角 8、永久伙伴 5、任务 NPC 3，路线牌另计。
6. Lv.1 与 Lv.20 的合法八牌配置均可开战、保存、恢复并继续校验。
7. 自动队列停在强制弃牌选择时，存档恢复后按相同字节状态继续。
8. 八牌法术任务停在检索选择时，存档恢复后按相同字节状态继续。
9. 最后一条已排队反击导致双方同时死亡时，最终判定为玩家胜利。
10. 七种主角构筑、100 个固定种子均在边界内结束：无目标卡手、手牌不超过 20、自动队列不超过 64、资源审计非负。

聚焦 100 场摘要：100 场均正常胜/负结束，共 133 回合、204 次主动出牌；气力消耗/获得 203/60，内力消耗/获得 153/13，护甲生成 720；最大手牌 5，最大自动队列 0，目标卡手 0；Direct 伤害 7,032，Reaction 伤害 968。

## 4. 2,400 场确定性观测

最终报告：

- `Saved/BalanceObservation/final_summary.json`
- `Saved/BalanceObservation/final_summary.md`

三次运行 ID：

- `run_20260811_044457_001`
- `run_20260811_044608_001`
- `run_20260811_045135_001`

三份 CSV 的 SHA-256 均为：

```text
a836e0ee715ef23c9b90f2cfdf2f31eaa0be1bcae2310384be14ae13d68bd9e6
```

代表批次：

| 指标 | 最终值 |
|---|---:|
| 胜 / 负 / 僵局 | 1,808 / 591 / 1 |
| 已决胜率 | 75.3647% |
| 回合中位 / P90 | 11 / 24 |
| 主动牌 / 自动结算 | 67,440 / 0 |
| 气力消耗 / 获得 | 65,961 / 62,747 |
| 内力消耗 / 获得 | 76,983 / 56,422 |
| 治疗 / 护甲生成 | 94,622 / 381,873 |
| 最大手牌 / 自动队列 | 5 / 0 |
| 目标卡手 | 0 |
| Direct / Reaction / DoT 伤害 | 2,846,090 / 12,459 / 3,717 |

目录审计为 198 张：主角 36、永久伙伴 108、任务 NPC 24、路线 30；品质为普通 122、稀有 47、珍稀 29。

与此前同矩阵基线相比：胜 1,814→1,808，负 584→591，僵局 2→1，回合中位 12→11，P90 25→24。变化很小，没有发现本轮主角机制造成确定性漂移或目标失效。

### 4.1 数据结论与后续平衡边界

- 功能性结论：`stranded_target_failures=0`，阵师/法师等条件牌没有因错误指向卡在手里。
- 稳定性结论：三次输出完全一致，随机流和续算可复现。
- 强度结论：这批策略仍是固定启发式，不等于真人最优策略；不能仅凭总胜率削卡。
- 明确待调项：`MixedMaxRegression` 与 `ShanHeTusi` 为 100% 胜；`NakedBaseline` 的各章首领为 0% 胜；唯一重复僵局为 `NakedBaseline|3|Elite|901017`。这些属于后续成长档、敌方数值和套装调参，不属于本轮功能回归失败。

## 5. 真实 PIE 与 UI 结构验收

### 5.1 完整流程

报告：`Saved/Automation/HeroCards_RealPlay.json`，`ok=true`。

实走路径：主菜单 → 新游戏 → 青山城镇 → NPC 剧情上下文 → 接受任务 → 永久商店购买 → 手动保存 → 城镇出口 → 路线图 → 起始节点 → 战斗节点 → Board 战斗覆盖层。

关键证据：

- 任务运行态和手动存档态均为 `ACCEPTED`，玩家保存位置误差为 0。
- 永久商店真实购买破军包：金币 10,000→9,900，装备仓库 6→7。
- 战斗位于当前生产结构 `L_RouteMap` 的 Board 覆盖层；运行时含敌我单位，牌区、共享气力、结束回合和单位 HUD 可见。
- 全流程 HUD fixture 仅写入瞬态 PIE 状态，截图后清除，不保存、不修改资产。

### 5.2 独立只读 HUD 观察

报告：`Saved/Automation/HeroCards_BattleHud.json`，`ok=true`。

- 视口：1530×722。
- 发现 4 个存活单位 HUD，敌我双方均存在。
- `validation_profile=live_structure`。
- `geometry_validation_mode=fixed_slot_fallback`。
- 最终 `errors={}`。

UE 5.8 Python 当前不暴露完整 Slate 几何 API，因此严格矩形重叠检查采用固定槽位后备证据；这是探针能力边界，不是对布局的修改。视觉截图 `Saved/Codex/real_flow_after_battle.png` 显示既有水墨战斗背景、意图、血条/内力、状态图标、手牌、共享气力和结束回合仍保持原布局。

## 6. 存档迁移

v12 迁移补齐法术任务运行态边界：

- 活跃任务继续迁移锁定牌、完成牌、首次顺序、首牌奖励和首牌拥有者。
- v12 以前从不存在的“非活跃但带残留任务字段”会被清空，避免恢复成当前版本非法运行态。
- 相同 v11 输入两次迁移产生字节一致的 v12 存档。
- 主角牌旧 ID、自动队列、牌堆/手牌/弃牌/消耗牌、暂停选择与独立战斗随机种子均继续保留或确定性迁移。

## 7. 已知规范冲突

仓库 `AGENTS.md` 仍把“接任务后 follower active，并保存 NPC 移动位置”列为旧 MVP 验收；当前生产代码则明确将城镇任务 NPC 设为固定角色：`AcceptTownQuest` 把 `bFollowerJoined` 与 `bHasQuestNpcLocation` 置为 false，NPC 不进入跟随状态，手动存档只持久化任务与玩家位置。

本轮没有擅自恢复旧跟随逻辑，也没有把旧门禁伪装成已通过。真实 PIE 报告验证的是当前生产行为。是否恢复跟随，或正式修改旧 MVP 规范，必须由用户单独裁决；它与本轮主角卡牌功能验收相互独立。

## 8. 最终范围声明

本轮完成并验收的是主角 36 张及其共享底层和诊断能力。全项目规划中仍标为“待落地/待单独评审”的伙伴新 18 牌池、NPC 双职业强牌、法师伙伴/NPC 小任务、青囊/追风/蚀骨套装重做、玄甲具体阶位、天赋、称号和最终六地势美术，不应因本文件而被误标为已实现。
