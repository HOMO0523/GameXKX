# 法术任务与药方状态栏：接入进度

状态：代码与素材材质已接入，冷编译和定向检查通过，尚未完成新机制条的真实 PIE 视觉验收。工作区 `codex/ui-visual-optimization`，沿用用户对此任务的批准。

## 已实现

- 独立只读机制视图读取真实任务、携带牌、首次出牌序号和已开启药方；不更改任务次数、伤害或奖励规则。
- 主角／伙伴／NPC 固定 4／5／3 个任务卡位，按携带身份固定位置、显示实际首次出牌顺序。未开启预留灰态；弃牌不视为完成，自动重放不追加主动牌序。
- 完成快照在战斗展示队列处理期间保留，随后短暂反馈再回到真实运行时状态；读档恢复不制造完成动画。
- 药方按来源卡保留编号，重复开方去重；说明读取开方品质，按已明确的药方规则显示本回合触发状态。
- 在资源条和普通状态条之间接入专用机制行；悬停复用 ItemSlot 纸底、江湖体和既有 Pill。标记短翻转，新增药方／进度轻脉冲。
- 雷、火球、雪花、气流和药纸使用此前选定草案。源文件原样保存；部分 RGB 草案仍含预览棋盘背景，由共用 UI 材质移除中性色底。不能把这些源文件称为透明 PNG 成品。

## 验证证据

- `Saved/Codex/CardEffects-20260907/mechanic-test-fixture-build.log`：GameXXKEditor Win64 Development 冷 UBT 成功。
- `Saved/Automation/BattleMechanics_20260908_Rerun/index.json`：7 项通过，6 项普通通过、1 项带警告通过，0 失败。包含固定牌位／归属、4／5／3 卡位、药方编号与品质／次数、完成快照，以及展示队列、资源条、状态 Tooltip。
- 初次运行中测试夹具把数组成员直接 Add 回自身数组，触发 UE 容器断言。已改成局部副本后添加，并冷编译重跑通过；该错误发生在测试构造，不是正常游戏开方操作。
- 地图、原存档及 HUD 倍率文件哈希保持。窗口位置为用户当前值，未恢复旧锚点。

## 未完成的验收

编辑器重新启动后，真实 PIE 验证开始前 MCP 连接已不可用、编辑器进程退出；`accept_mechanics_live.py` 在只读导出步骤失败，尚未导入测试配装。没有本轮新状态栏实拍，不宣称运行时遮罩、最终小尺寸或悬停命中已通过。

继续时检查：实际开任务／开药方与 Tooltip、元素切换、最后一张牌重放、原始棋盘底移除效果、75% HUD 与较小窗口下数字可读性、额外机制行是否挤压资源和手牌区域。完整法师特写仍属后续设计项。本轮没有新 Game Development 构建或 Cook 验证。

出牌策略与怪物强化仍按 `2026-09-07-combat-strategy-balance-todo.md` 保持待做。
