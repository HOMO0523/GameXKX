# 路线视觉接续

当前地图任务已经交接，不要再使用旧 `route-title-before-layout-fix.png` 当最新基线。最终实际图为 `Saved/RouteNodeArt/map-verified-w6.png`，对应 `docs/production/2026-09-09-route-map-travel-money-acceptance.md`。

最新图已把左右原先失真的小纸片取消，整窗变为浅纸色，保留中间水墨山水。左上摘要与八类图例不重叠；**右侧八条灰色描述和底部两条灰色状态说明仍显示**，这是本轮继续修改的直接对象。

接续方案：

1. 保留对方最终全屏浅纸底、节点图标、画圈动画、静态已选圈、金币与行旅钱语义；不恢复已移除的小纸片，也不调整节点坐标来补偿图例问题。
2. 右侧每行保留图标与短名称；八条描述移到该行 tooltip，底部“墨圈/淡色”说明移至图例标题 tooltip。行高只容纳单行，但使用实际字体 desired size，不硬套名义 point size。
3. 目前 `RouteLegendContainer` 及 Tick 设为 `HitTestInvisible`，会连同子控件一起禁用悬停；改为允许子项命中的容器，并让行作为可悬停区域。不要制造整块透明挡板覆盖地图。
4. 保留左上关名、行旅钱与行程数字；补对应中英文本与说明。左右既有额外纸张 Brush 改为明确无绘制，避免以后样式复用时又出现小尺寸拉伸边缘。
5. 中英文短名称和完整 tooltip 均应从同一词库读取；切语言后已打开的 tooltip 和图例一起更新。检查右侧 48/64 等实际图标尺寸，不为了英文把文字无限缩小。

前置：当前共享 UBT 尚在运行，根 Source 已冻结。此文件是接续计划，没有改 RouteMap C++。
