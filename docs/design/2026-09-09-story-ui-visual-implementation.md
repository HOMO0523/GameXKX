# 主线与天赋视觉落实

沿用已确认的横向树、江湖体、粗墨线、图多字少与角色独立对白要求，当前分支许可继续有效。图片重绘仍走逐张确认，本轮使用现有节点素材安排展示，不批量生成。

- 纸面：对白、主线与背包走同一DesktopPaperStyle，固定纸边视觉尺度；文字与按钮留在纸张安全内边距，禁止把整个大纸图直接压扁。
- 对白（按用户最新截图纠正）：纸框从734延长到924逻辑宽，左侧为框内半身像，右侧为名字与正文；旁白/画外音没有专属头像时铺开文字区。半身像使用项目原立绘的上半身UV窗口，不覆盖角色资产。**幽白是火灵，单独显示完整火焰与尾部轮廓，不使用人形半身裁切。** 正文留行距和安全边距，隐藏粗墨滚动块、保留滚轮阅读，去掉默认阴影线；提示仅在调查选项时出现，普通对白保留轻量继续提示。
- 主线：横向剧情画廊式流程。每节点以3:1插图、短标题和状态印记呈现；分支上下分道，墨线在图卡后绘制，已完成/可进行/未开放区分颜色与粗细。节点详情上方大图，下方简介和目标，奖励与开始操作收在页脚。
- 天赋（最新裁决）：用户更喜欢原四向展开，已回撤本轮四页签/横向天赋布局，只保留精简文字、江湖字体、墨线和生命周期修复。主线仍为横向图卡。
- 合计属性（用户新要求）：右侧增加“合计属性”按钮，切换为只读分组列表，返回时保留原选中天赋。按已学天赋统计有效加成，固定值与百分比区分；复用TalentProjection的合并与上限，容量剔除基础值和旧存档保底。非零条目按基础战力、游历战力、行囊容量、游历收益、离线收益、宝箱收获、百工收益七组排列，组间分隔、数值右对齐，可滚动查看。只有统计与展示，不写进度或扣金币。
- 验证：冷UBT，主线/对白/天赋及工作台相关检查；默认2D隔离PIE实拍图卡树、对白、选项与天赋，并重复关闭/切页/GC。此前35+79项证据不能代替本次变更后的验证。

首次视觉版冷UBT通过，115项中114通过；唯一失败为旧天赋存档测试硬编码版本36，已按天赋引入版本修正断言，等待最终复验。实机首轮发现图卡边框过重、状态文字与边缘混杂、滚动条过粗、章节列表仍含非江湖字体，已继续修改。用户又确认加宽纸框并改框内半身像；该轮正在冷编译，不能把首轮截图冒充最终效果。

实机使用`Saved/StorySystem/VisualV2User`隔离存档。一次MCP启动失败实际为端口绑定失败，已通过编辑器控制台重新启动MCP恢复；未强制关闭未保存编辑器。后续命令行自动化测试改用独立MCP端口，避免与视觉编辑器18765冲突。

## 当前构建与检查证据

- 首次全量构建期间，同项目另一任务改动宝石/存档接口，产生旧声明缓存。没有回退这些代码；重新触碰SaveMigration.h时间戳后，57个相关编译单元重新读取v39接口，`visual-v2-schema-refresh-build.log`通过。
- 用户允许后，已与“统计天赋金币需求”协调并获得暂停MCP、输入、编辑器、UBT和共享接口的确认。对方仍有未编译的元素相关字段，属于其后续工作。
- 最后的幽白裁切/滚动提示调整只涉及DialoguePanelWidget.cpp。保存关闭编辑器后执行真实UBT单文件编译，再用已验证模块的链接响应文件仅替换该对象进行正常冷链接，没有使用Live Coding或Hot Reload，也没有编入另一任务未完成的元素接口。依赖文件确认不含其四个待编译头文件。证据与基线/新模块哈希在`Saved/StorySystem/ColdDialogue/`。
- `Saved/Automation/StoryVisualAccepted/index.json`：115/115通过，0错误；3条既有可选1K图集回退警告单列。对方新增宝石装配用例不计入这一UI验收，已向对方交接其夹具失败；我仅修正过三条TestEqual期望值的数值类型，以解除编译歧义。
- 当前验证对应已编译的UI模块；不能把另一任务尚未编译的元素字段称为已构建或已验证。整合这些字段时仍需由其执行完整冷构建。

用户随后要求恢复四向天赋并追加合计面板，已完成统一完整冷构建，日志`Saved/StorySystem/talent-totals-build.log`（387个动作）。此次编入另一任务已配套完成的共享接口，前述单文件冷链接仅为历史证据。`Saved/Automation/TalentTotalsFinal/index.json`为118/118通过、0错误。新纯汇总模型为`UI/GameXXKTalentTotals`；三项用例覆盖兼容容量排除、完整天赋同类合并/单位/上限、面板只读切换。实机验证了右侧分组列表与返回按钮；随后补充缓存天赋重新打开时读取最新等级的回归。

用户追加反馈“关闭背包时挂机条瞬间跳变”。已通过实际Slate绘制重现：75%关闭首帧宽度由778.5瞬间变成1254，下一帧恢复。原因是保持挂载的RootScaleBox仍使用ScaleToFit，在参考画布尺寸切换时依赖上一帧Paint区域；改为使用布局解析器的当前比例，并保留桌面DPI换算。未修改挂机进度、动画状态或锚点公式。

- 首帧回归：`Saved/Automation/IdleCloseRedAuthority/index.json`在旧代码下12次首帧断言失败，其余稳定帧通过；覆盖50/75/100缩放与各4次开关。早期两个测试夹具没有实际切换运行时状态，不能算作复现证据；最终夹具同时断言运行时就绪、实际缩放值、展开状态切换和非零绘制尺寸。
- 修复后：`Saved/StorySystem/idle-close-fixed-build.log`冷UBT通过；`Saved/Automation/StoryUiIdleCloseFinal/index.json`119项中118通过，唯一失败为旧结构测试硬编码ScaleToFit。仅更新该断言要求解析器比例，`idle-close-contract-build.log`冷UBT及`Saved/Automation/StoryUiIdleCloseContract/index.json`单项复测通过，合计119项通过。三条既有可选1K图集回退警告、一次联网探测超时不属于本次关闭修复。
- 最后构建也包含缓存天赋重新打开读取最新等级的修正，新增等级刷新断言通过。
- 已恢复“统计天赋金币需求”的共享接口写入权；其后续17宝石补丁不计入本次已编译模块验收。

## 最终实机回归

使用`Saved/StorySystem/TalentTotalsUser`隔离存档、默认纯2D地图。`Saved/StorySystem/stable-ui-verification.json`与`final-ui-lifecycle.log`记录三轮真实鼠标流程：任务→首章结果→回看前四句→关闭对白/主线→天赋/编队→GC→再次切换。三轮结果图均加载、每次继续只前进一句、对白逐句不重建全局、主线进度不变。75%与50%下实际点击“关闭背包与全部子界面”后，展开状态均为false。

在隔离存档通过公开购买接口学习根节点、战斗入口和攻击节点，重新打开缓存天赋图后等级正确显示1/1与1/5；点击合计属性显示攻击+10、生命+5、防御+5与仓库+1页，同类合并且单位分开。返回天赋按钮实点有效。最终截图为`Saved/StorySystem/talent-total-attributes-final.png`、`idle-closed-final.png`。此前对白最终实拍`final-hero-bust.png`、`final-youbai-full.png`仍有效。

119项合并检查与模块SHA256记录在`Saved/StorySystem/idle-close-verification.json`。测试结束已恢复全局HUD缩放50%，停止PIE并通过MCP保存（无脏包），将编辑器操作交还协调任务。
