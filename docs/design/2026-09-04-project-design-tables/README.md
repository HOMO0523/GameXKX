# GameXXK 设计总表与配队分析

装备总表已于 **2026-09-09** 按当前工作区代码核对更新。文件名保留原日期以维持已有链接，工作簿内记录本次更新时间、分支、HEAD 和源码 SHA256。

- [装备设计总表](GameXXK_装备设计总表_2026-09-04.xlsx)：35 条词缀采用当前统一装备提示的效果文本；30 条分系词缀逐条记录触发边界、接入状态和审查建议；49 个装备模板名称、18 条套装完整效果均已对照源码。
- `04_词缀目录` 默认打开，名称与运行时状态相邻；`04A` 记录审查建议，`04B` 记录当前品质区间与方案单条名义数值，`04C` 记录新旧显示入口。
- `04D` 记录分系百分比的边际模型：先乘 **0.5**，同一次同类收益汇总为 S，再按 **E=75S/(75+S)** 折算，实际额外收益不超过 **75%**。`04E` 给出10档词缀各1～6件的60组例子，`04F` 是递减曲线。产甲已接入共享结算，其余29条词缀仍待触发定义与接入；`04G` 记录详细属性按钮、最终值和条件效果的显示口径。
- `02_宝石总表` 按[2026-09-09品质修订](../2026-09-09-gem-quality-marginal-curve.md)更新：攻防 `1、2、4、8、16、32、58、95、137、172`，生命5倍；不朽后升级倍率从×2均匀降到×1.25，每阶向上取整，属性总值继续增长。多颗正常相加。`02A` 展示品质倍率和增量，`02` 并列当前代码值与差异。先更新文档，再同步项目数值。
- `07_掉落等级带` 保留批准范围，并列出当前 100 级上限与 SourceItemLevel 规则。
- `02`现为14类×10品质，共140条；`02B`逐档列出属性宝石（0.25%起）和机制宝石（0.5%起）的名义值与75%边际后的单颗有效值；`02C`记录伤害来源、叠加池、品质Shader和镶嵌文本规则。
- `07A` 记录已实装的普通金币 ×10、困难至地狱每关 ×1.25；`07B/07C` 记录尚未实装的分解基础价原方案 ×5，以及仍待确定的工具经验方案。
- `01_百级最终属性` 与 `06_至宝配装基准` 同步新至宝宝石贡献：+128攻击／+128防御／+640生命。角色设计总值只随宝石比2026-09-04增加48攻击／48防御／240生命，原值另列对照。源码已接入、没有消费者、设计待实现、待确认分别标注；设计总值不是实机同值验收。

装备表维护使用专用脚本（需要 `openpyxl`，可用工作区依赖 Python）：

```powershell
python scripts/update_equipment_master_workbook.py
python scripts/update_equipment_master_workbook.py --check
```

更新前会备份到 `Saved/Diagnostics/EquipmentMasterTableUpdate/`，保存后重新加载并核对 ID、名称、数量和批准基准。历史 `export_game_design_tables.py` 会按旧结构重建装备表，不应用它覆盖本次总表。

以下其他文件仍是各自原始快照，本轮未更新或重新验证其内容：

- `GameXXK_卡牌设计总表_2026-09-04.xlsx`：173张卡、419个品质版本、36条分支、1498条逐卡Pill说明。
- `GameXXK_怪物与阶段数值设计总表_2026-09-04.xlsx`：批准的27关等级/189编制、21怪物、351个难度/阶段意图用例，并单列125级地狱3-1。
- `GameXXK_职业配队与伤害期望分析_2026-09-04.html`：100级新装备角色对125级地狱3-1的透明设计期望模型。

原卡牌表标注的两项未决为 `Profession.Sorcerer.RanLingHuanYuan` 倍率与 `Npc.JinGui.HouXiangTuoShen` 对象语义，该标注不代表当前代码仍未实现。配队分析继续作为对应生成日期的设计模型阅读。

其他历史导出入口：

- `python scripts/export_game_enemy_design_table.py`
- `python scripts/export_game_analysis_html.py`
