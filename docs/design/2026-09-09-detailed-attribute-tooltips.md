# 详细属性悬停提示

主列表保持 18 个单行属性，不恢复解释文字或来源分组。每条属性整行都可触发提示，滚轮继续交给原列表。

提示采用当前纸质 tooltip 样式与江湖体，分为三个区域：

1. 属性名称与当前最终值。
2. 计算规则：说明适用范围、75% 边际递减、相乘或相加顺序。
3. 加成来源：每类单独一行，列出当前角色的实际来源值。

三个区域用细分隔线与留白分开。抗性列基础抗性、对应宝石名义值及实际增量；护甲获得列词缀、宝石、固定套装，并说明共享边际；物理反击与灼烧明确说明其两类宝石合池。当前装备确实持有但未生效的词缀只在提示中注明不计入，不虚增最终值。

来源发生变化但四舍五入后的最终值不变时，提示也必须刷新。换角色和换装后提示须对应当前角色。正文和所有数字都使用 `FGameXXKInRunUiStyle::Font(..., true)` 的江湖体。

3 份 cpp 的局部补丁已应用，备份和哈希保存在 `Saved/Diagnostics/DetailedAttributeTooltips`。前轮自动化状态快照仅作历史记录；最新完整原始报告使用下述本轮报告。

主列表仍无解释文字；新增测试覆盖三段结构、整行悬停、江湖字体，以及宝石名义值从473.25%变为473.50%而最终值均为64.74%时的提示刷新。

2026-09-10 验收：最新冷 UBT 通过；`Saved/Automation/TalentUpgradeUsability20260910/index.json` 中包含这6项属性检查，全部成功（联合25/25、0警告）。真实鼠标点击“属性→详细属性”，分别悬停“暴击率”和“物理伤害加成”，已确认标题/最终值、计算规则、加成来源的分隔与江湖体，切换行时内容跟随更新。图片位于 `Saved/Diagnostics/TalentUpgradeUsability/attribute-tooltip-critical.png` 和 `attribute-tooltip-physical.png`；主列表图为 `attribute-main-list.png`。临时试验会话已恢复、正常保存，PIE 停止且脏包 0。
