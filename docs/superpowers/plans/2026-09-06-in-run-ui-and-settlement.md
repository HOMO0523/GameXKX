# 局内 UI 与 Boss 整关结算实施

用户已于 2026-09-06 确认“ok，优化吧”。在根目录 `codex/ui-visual-optimization` 执行，不创建 worktree，不使用子代理。范围基于 `docs/production/2026-09-06-in-run-flow-ui-audit.md`。

## 目标与边界

- 修复当前真实流程的战后遗留发药、遗物栏缺失。
- 当前单关 Boss 胜利原子发奖并保留结算凭据；显示整关结算，确认才回工作台；读档不重复发奖。
- 重做商店、环境事件/宝匣/篝火、Boss结算的纸面和排版；提高卡名、价格、费用、关键说明占比。
- 普通战后奖励继续走现有三选一；Boss结算不是三选一。
- 不调整两张待定牌、遗物强度、事件收益、装备数值曲线。结算显示当前真实发放的奖励；装备135属于后续数值计划，不能在UI伪造。
- 保留已有手调资产与工作台字体试用改动，基线存于 `Saved/Codex/RouteUI-20260906/implementation-baseline`。

## 执行阶段

- [x] 1. 增加真实入口回归，复现奖励后发药与桌面遗物栏缺失；冷UBT证明RED。
- [x] 2. 修复发药、遗物栏与退役测试入口；范围回归GREEN。
- [x] 3. 实现当前v36之后的Training结算凭据/统计/确认/恢复；先测试后实现。
- [x] 4. 接入结算Widget与玩家流程，统计明确标注Boss战口径，奖励与成员成长来自冻结凭据。
- [x] 5. 统一商店、事件、结算、相关卡牌的样式与布局，整理战斗遗物排列与溢出查看。
- [x] 6. 冷UBT、相邻自动化、真实2D PIE完整流程及不同缩放/窗口实拍；更新验收和当前指针。

## 版本和结构适配

当前生产存档为v36；旧Plan6假定的v37→v38前置不成立。本任务占用下一个实际版本v37，引入Training结算状态和统计；迁移旧档只初始化空记录，不依据历史通关补发奖励。后续成长计划使用届时下一个版本。

沿用批准Plan6的Build/Apply/Acknowledge语义：奖励和进度先原子提交，确认只清理展示凭据与返回；不依赖UI重新计算奖励。保留现有路线经济清算，避免两套结算重复发钱。统计按实际战斗提交记录，冻结时标明为Boss战；不冒充整关累计。

## 关键验收

- 普通/精英奖励领取、跳过不产生Item.HealingPowder；重复回调不重复收益。
- 桌面保持惰性启动，进入局内并持有遗物后出现六列遗物栏；多行可查看且不挡战斗操作。
- 篝火保持当前30%恢复/100行旅钱，药品数不变；旧“护符”断言按已确认休息规则校正。
- Boss只在最终阶段胜利后生成唯一凭据；奖励、首通、解锁、成员经验正确。
- 结算前未确认的存档恢复同一页面；重复确认、重开不重复发奖；错误时保留可恢复状态。
- 商店仍为四强化＋四遗物，使用普通金币，已售状态和刷新正确；事件确认及未结算返回不漂移。
- 卡牌名字/费用/说明不压住图片，长文字不裁切；50/75/100%及窄窗口用实时截图核对。
- 任何C++验证先MCP保存脏包，冷UBT，不用Live Coding或仅查日志代替编译。

阶段1证据：冷UBT通过；RED为3项中2项按预期失败（发药、遗物栏），GREEN为38/38，0错误，111条既有夹具/展示诊断警告保留。报告：Saved/Automation/UI_Phase1_RED_20260906、UI_Phase1_GREEN_20260906。

2026-09-06用户实拍反馈已纳入：商店取消卡名方形色底；卡名和费用恢复江湖体，与局外统一；立绘恢复原190×228内容范围，费用改为左侧独立两行。长遗物说明使用显式换行宽度。

额外实跑修复：清除局内上限加成时将气血/内力收回永久上限；单关Training的chapter=1节点收据允许保存，不误判为旧三章激活数据。

本轮优先重做用户指定的商店、事件/宝匣/篝火和Boss结算。普通奖励整体遮挡、战斗单位信息与退出遮罩（U05/U06/U09）继续保留在整体审查表，不宣称全部21项完成。

验收完成：核心96/96、最后UI25/25，冷UBT及两条真实路线、保存/重开/确认/多窗口实拍通过。详见 docs/production/2026-09-06-in-run-ui-settlement-acceptance.md；扩展基线未通过项另列，未冒充全项目全绿。
