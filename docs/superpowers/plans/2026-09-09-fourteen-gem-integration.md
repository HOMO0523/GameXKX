# 14种宝石接入

用户已确认采用14种并继续接入，复用本任务`codex/ui-visual-optimization`分支授权。原图审阅关卡已通过；不再生成9档品质图。保护原资产、存档与默认2D地图，不使用worktree、Live Coding或UnrealBridge。

## 验收范围

1. 14类×10品质的稳定道具ID可解析、获取、合成、镶嵌及保存。旧三类ID和枚举值保留。
2. 每类只引用1张已通过的图标。名称、底面、tooltip复用装备品质Shader；装备上的每颗镶嵌宝石标签按自身品质显示。
3. 固定攻防曲线1/2/4/8/16/32/58/95/137/172，生命5倍。3种基础百分比值为该曲线×0.25%，各属性分别以75S/(75+S)折算，对裸身＋装备＋固定宝石生效，再计算永久天赋。
4. 8种机制宝石按来源角色结算：直伤、产甲、治疗、反击、火焰、持续伤害、冰霜、雷击。它们增加现有结果，不增加次数、层数、消耗或循环。用户已确认机制名义系数为曲线×0.5%；基础百分比仍×0.25%。
5. 同次伤害的通用/元素/持续专精先合并名义值，最多一次75%边际；固定天赋仍走现有规则。产甲宝石与已实现的随机产甲词缀合并收益池，随机词缀继续先乘0.5；固定套装独立。
6. 毒/血/灼烧记录实际施加者贡献；自然跳伤按各贡献者加成，毒爆/引燃保持来源边界并不重复放大。旧档无来源的剩余状态保留基础伤害，不冒认某个施加者。
7. 详细属性显示实际有效值与明确条件，不能把未实现的装备随机词缀标成生效。

## 执行顺序

- 先写接入规格和数据表；建立可观察的目录/投影/品质显示失败用例并冷编译运行。
- 导入14张固定图标；扩展枚举、道具目录、数值、获取及装备投影。
- 补齐战斗个人宝石快照、伤害来源/元素、状态贡献与结算；保持存档兼容。
- 接入统一的宝石品质展示、独立tooltip与混合品质镶嵌文本，更新详细属性。
- 冷UBT、相关规则与存档/界面自动化、2D真实PIE及源文件保护检查。此前2条已修正测试也一并复验。
- 更新装备总表及进度记录，明确已验证范围。

## 约束

- 不顺带启用其余29条缺少消费者的装备随机词缀，不更改分解定价或工具经验。
- 不新增速度/内力上限宝石，不用光环/皇冠造型区分品质。
- 图标通过v5清单定位，保留旧30张资源，禁止删除或覆盖用户手调资产。

## 后续用户指示

继续分析并为适合的角色卡和怪物招式标记冰霜、雷击、火焰伤害；在现有竖向落雷表现链上接入冰霜与火焰六帧命中特效。普通兽类物理攻击保留；不自动增加减速、冻结、易伤等新状态。两组特效已选B02/042与B02/039作动作参考并完成内置imagegen重绘，待表现链验证。
