# Dev 配装与真实战斗数值报告

用户已授权本次 Dev 工作继续在 `codex/ui-visual-optimization` 根工作区完成。保留已有 UI、资产和原玩家存档；不改平衡参数。

## 实施顺序

1. 首页一键配装：统一可选等级（默认100），至宝/+10、合法中位词缀、同品质12孔4攻4防4生命。按用户指定职业映射；主角默认破军可选，NPC采用文档第一关联职业。重复调用复用匹配装备；蚀骨六件在物理背包。失败原子回滚。
2. Dev 大纸底采用背包的背景单独缩放；栏目下拉使用 ItemSlot。真实 PIE 截图复核。
3. 修正模拟输出/承伤混计，增加逐步实际伤害包、资源和状态快照、治疗/产甲事务记录。统计使用同一战斗适配器和结算账本。
4. 增加可复用的标准阵容场景构造及同步单场运行接口，方便 AI 批测和自动化。标准场景从新游戏创建，隔离玩家天赋/遗物/存档；记录全部合法卡组和装备。
5. 运行匹配种子的职业×NPC矩阵、多关卡基准和出牌联动对照。出牌器为现有 Skilled 贪心策略；不声称最优玩法。错误/上限/僵局与真实胜败分开，保留原始数据。
6. 生成离线 HTML 报告，提供可筛选热力图、DPR、角色贡献、逐牌联动证据、方法与限制、优先级明确的调整建议。附 JSON/CSV、构建指纹、代码哈希与复现命令。

## 验收

### 用户追加范围（2026-09-07，继续同一任务）

- 以 `docs/design/2026-09-04-project-design-tables` 中怪物、装备、卡牌三张 Excel 为核对对象，记录工作簿 SHA-256、逐行比较及语义专项测试。旧 A 版全集只作历史参考。
- tooltip 实拍后修正字号未生效、候选数量误写为选择数量、三行预算丢失机制，以及数值替换丢失目标/护甲消费条件。
- 战斗气力图标、立绘下方血/内力条、按钮江湖体、意图卡标题及伤害数字整体排版优化，真实截图复核。
- 所有伙伴及NPC升级/星级不再增加内力；明确取代旧的仅主角/法师固定内力范围。战斗卡牌和遗物的显式临时增量继续保留。
- 扩大实测发现意图预演复制了玩家手牌费用绑定，以及回合结束DoT转阶段后复用旧阶段意图；修复后重跑原失败种子与矩阵。`20260906-runtime-matrix` 的部分样本保留为诊断基线，不混入最终数值结论。

- RED 已实机确认 `equipment.recommend_all` 不存在，证据在 `Saved/Codex/DevRecommended-20260906/red-recommended-command.json`。
- 覆盖配装映射、等级/孔位、背包蚀骨、重复不增殖、失败原子性与原档保护。
- 冷 UBT Editor 和 Development 游戏目标；针对性自动化、固定种子重跑一致、逐包伤害与汇总对账。
- MCP 保存脏包后关闭编译；重启在纯2D默认地图做真实UI和战斗检查。
- 报告展示样本数/失败分母/策略边界。数值建议不自动写回游戏。
- 结束恢复原会话并核对本轮备份中的玩家文件与地图哈希。
