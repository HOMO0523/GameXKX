# 剧情插图按章重绘与对白半身像

用户最新决定：剩余60张按章重绘、每章一起验收，画各节点精彩部分并使用不同布局/构图。此前逐张生成前确认已被这次决定替代。S02-02已通过且导入，保持该成品。

用户追加：有台词的剧情角色需要半身像，切好并放进对话框。已有项目主角/主要角色采用对版原设；幽白保留完整火灵；补齐12位缺少专属头像的配角。每章的人物设定和对白头像先建立，再将同一头像作为本章插图的人物依据。

1. 修正出图生产提示：以已通过的带笔触船夫图为画风依据，撤下未认可的纯平涂参考；主角短圆脸、粗眉大眼与服饰锁定项目原设。
2. 第一章S00先出车夫、山客、住持、掌柜四张透明半身像，再出七张各有镜头的剧情插图。7图分别为低机位追图、近景灵火现身、俯看桥板翻落、护稿战斗交叉动作、俯视旧图补丁、侧近景幽白照笔、低处旧桥凳脚揭底。
3. 每张绑定现有节点剧情事实与结果，不改任务前置、奖励、旅程次数或存档规则。后续章尤其补齐S05尚空缺的高光/镜头方案。
4. 项目交付图1536×512，审阅WebP，游戏PNG源→BC7，使用已验证的篝火式四边渐隐。对白半身像保留透明边缘，切图后用统一尺寸与人物底线放入既有纸框。
5. 本章齐套后向用户一起展示插图与头像，按章收反馈；通过的资源替换原路径，半身像接入角色映射并做冷UBT/实际对白切换检查。未通过稿不标作最终交付。

当前：第一章制作中。旧61张不算新画稿完成；其余章在本章验收后继续。生成使用内置image_gen，不调用外部CLI。

最新角色反馈：四张首版配角半身像均被用户否决，原因是脸型同质、皱纹/衣纹偏写实，与项目NPC不符。停止使用这些头像作为插图参考。重新直接以土司、宋金宝、金贵原设的图形化造型为依据：车夫方厚横向、山客瘦长折线、住持圆形、掌柜倒三角；独立眉眼鼻口和头饰，不靠换衣服区分。首版切图也未通过（背景仍被保留），严禁导入；新稿需真实alpha并检查轮廓内的透明孔与边缘。

## 当前实际进度

**最新用户验收：第一章插图全部通过。** 七图和同批四位对白角色已记录用户通过；S00-07节点叫“客栈”，天台山这家店的四字招牌名为“傍山客栈”。用户新增世界观：所有城镇都有外观相同的客栈与掌柜，仅四字店名不同。见`docs/design/2026-09-09-recurring-inn-world-setting.md`和`SourceAssets/Narrative/World/recurring_inn.json`。后续每章继续同样的分章制作/验收方式。

统一协调后的ForceHeaderGeneration完整UBT与13项合并检查现已全过，0失败0警告（`Saved/Diagnostics/DetailedAttributesCompact/unified-build.log`、`Saved/Automation/DetailedAttributesCompact20260909/index.json`）。其中三项属于此任务的对白/头像检查，前述UHT和测试尺寸问题已解决；实机头像截图与首章七图导入正在收尾。统计天赋任务先完成其短截图后交回编辑器。

**本章实际接入已完成：** `Saved/StorySystem/S00-chapter-import.json`确认7张通过稿已替换原纹理路径，均1536×512、BC7。四位对白角色已通过真实节点回看显示：`S00-driver-dialogue.png`、`S00-mountain-man-dialogue.png`、`S00-abbot-dialogue.png`、`S00-innkeeper-dialogue.png`，均在`Saved/StorySystem/`；半身像留在纸框内，真透明且身份对应。`S00-approved-tree.png`显示新图、软边及“客栈”标题。实机采用项目可恢复开发快照，检查结束已restore、还原50%HUD、停止PIE并MCP保存，不作为实际完成首章任务的游玩证明。

输入检查中发现桌面DPI被探针重复换算：Slate子控件pos已是物理像素、根窗口size仍是Slate单位；已修正`main_story_ui_check.py`为DPI-aware并直接使用子控件pos与控件实拍尺寸。另有滚动位置复位导致选不到离屏节点，最终显式滚动后四个目标均已实拍，未将失败尝试算作通过证据。

当前编辑器PID42856已交音效任务，随后直接交地图任务；本任务不再占用编辑器/编译窗口。第一章7图+既有已通过S02-02共8张新图已接入，其余53张仍按章推进。剩余8位配角头像尚待相应章节制作。

- 第二版四个NPC造型已生成；住持按用户最后反馈改为一颗大中心念珠（abbot_v4），其他三位为v2。512×512真RGBA切图已核对并导入StoryPortraits，每张BC7为262144字节；角色映射接入四个新路径，已有主角/幽白/主要NPC路径保留。新版未冒充用户整章最终验收通过。
- S00七张新图已齐：s00_01_v5（纵深/动势）、s00_02_v4（减杂）、s00_03_v5（鞋印+桥梁构造）、s00_04/05/06/07_v3。母版和逐张生成提示位于`SourceArt/UI/StoryNodes/Redraw/S00/Raw/`。
- `review.html`提供整章图、旧图对照、四周柔化开关与四位对白头像预览；`contact-sheet.png`提供整章总览。七张1536×512 WebP共1148046字节。主manifest已标为agent视觉检查通过、user_review=pending、review_batch=S00，等待导入实机预览和用户整章验收。
- 首轮三项对白检查中两项通过；新增用例所有角色/路径解析正常，但NullRHI下GetSizeX/Y为0。已改读Texture.Source并修正int64期望类型；实际512/BC7资源另有MCP证据。
- 当前完整冷构建还未通过：`S00-portraits-linked-build.log`出现路线控件UHT宏行号滞后（旧127/156，当前136/165），与其他地图/音效任务并行写入相关，不应修改手写头去迁就旧生成文件。已请求用户授权向额外两个任务协调编译窗口，尚未把UI验证标成完成。
- 《统计天赋金币需求》已冻结其3个详细属性cpp，等待这边通过后用独立18866端口跑其NullRHI检查；地图/音效任务的协调授权还在等待。不要启动重复编译或旧DLL的单TU混合链接。
