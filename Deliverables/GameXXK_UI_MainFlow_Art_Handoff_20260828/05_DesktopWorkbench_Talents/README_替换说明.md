# 05_DesktopWorkbench_Talents UI 美术替换说明

只修改 `02_Assets` 中的 PNG；文件名采用项目内 UE 资源名。保持画布尺寸和 Alpha，回填时按 `asset_manifest.csv` 的 `ue_asset_path` 确认目标。

`01_Runtime_Screenshot` 是实机状态；`03_Annotation_Guide` 是区域与素材类别对应图。文字、数值、纯色块和程序边框标记为 `CODE_DRAWN`，不要烘焙到底图。

本包不自动导入 UE，也没有修改项目代码、地图或 `.uasset`。
